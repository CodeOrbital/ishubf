# Sparkling Solutions
## About the Company
### What Is It?
- This is an imaginary company made for the ISHub Front End Summer Camp 1st assignment, as indicated by the folder '**first**'.
- It is supposed to be a tech company that delivers digital products and services.
- It is supposed to work in:
  - Education
  - Health
  - Business and other fields
### More Information
- [General information](index.html)
- [About the company](about.html)
- [About products](products.html)
- [About services](services.html)
> We offer different digital services and products for your needs.
---
## About the Project
### Language Usage
1. Because the assignment strictly instructs us to use only HTML, I used only HTML5.
2. This means I didn't use any type of CSS code—not even inline CSS—nor JavaScript.
3. But to make it as aesthetic as possible, I used different HTML formatting and aesthetics-related elements and attributes, such as:
   - Elements:
     - `font`
     - `hr` (horizontal rule)
     - `br` (break) for spacing (since I shouldn't use padding and margin)
     - `img` to add logo and icons
   - Attributes:
     - `width`
     - `height`
     - `bgcolor` (background color)
     - `background` to add background image
     - `color`
     - `face` in the `font` element
     - `size`
   - **On tables:**
     - `cellpadding`
     - `cellspacing`
     - `bordercolor`
     - `border`
## Notes
> There are some things that are deprecated yet used because of the absence of CSS.
- Using obsolete HTML aesthetic elements and attributes instead of CSS
- Since I couldn't use CSS to configure the font size of texts, I used the `font` element to wrap other elements even though it is an inline element.
- Since I couldn't use padding and margin properties, I used many break elements (`<br>` tags).
- In index.html, I wanted to remove the **underline** from *Sign In* and *Sign Up*, but because I couldn't use the `text-decoration` property, I made the anchor elements wrap buttons, though they are inline elements.
## ***Understand!***
> ***This is done because of the assignment requirements, not by preference or because of failing to follow conventional coding practices.***